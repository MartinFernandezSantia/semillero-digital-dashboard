'use client';

// Esta página se encarga de redirigir automáticamente a la sección apropiada
// El layout del dashboard maneja la lógica de redirección

export default function DashboardPage() {
  // El layout se encarga de toda la lógica de redirección
  return null;
}
